import React from 'react';
import NewAccountContainer from "../../Containers/NewAccountContainer";

const NewAccountLayout = (props) => <NewAccountContainer {...props} />;

export default NewAccountLayout;
